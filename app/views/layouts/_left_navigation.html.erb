<nav class="navbar-default navbar-static-side" role="navigation">
    <div class="sidebar-collapse">
        <ul class="nav metismenu" id="side-menu">
            <li class="nav-header">
                <div class="dropdown profile-element">
                    <img alt="image" class="rounded-circle" src="<%= image_path('profile_small.jpg') %>"/>
                    <a data-toggle="dropdown" class="dropdown-toggle" href="#">
                        <span class="block m-t-xs font-bold"><%= session[:user_name] %></span>
                        <span class="text-muted text-xs block"><%= session[:role] %> <b class="caret"></b></span>
                    </a>
                    <% if is_admin? || is_sme? %>
                        <ul class="dropdown-menu animated fadeInRight m-t-xs">
                            <li>
                                <a class="dropdown-item" href="<%= url_for :controller => 'users', :action => 'profile' %>">Profile</a>
                            </li>
                            <li>
                                <a class="dropdown-item" href="<%= url_for :controller => 'users', :action => 'update_password' %>">Change Password</a>
                            </li>
                            <li class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="<%= url_for :controller => 'login', :action => 'sign_out' %>">Logout</a></li>
                        </ul>
                    <% end %>
                </div>
                <div class="logo-element">
                    IN+
                </div>
            </li>

            <li class="<%= is_active_controller('dashboards') %>">
                <%= link_to dashboards_path do %>
                    <i class="fa fa-th-large"></i> <span class="nav-label" data-i18n="nav.dashboard">Dashboard</span> 
                <% end %>
            </li>

            <% if is_admin? %>
                <li class="<%= is_active_controller('users') %>">
                    <%= link_to users_path do %>
                        <i class="fa fa-user"></i> <span class="nav-label">Users</span> 
                    <% end %>
                </li>
            <% end %>
        </ul>

    </div>
</nav>